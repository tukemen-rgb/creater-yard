# CreatorYard SPEC（MVP）

2026-08-06 起草。決定の出どころは GAMEYARD リポジトリ
`docs/creatoryard/CEO_REVIEW.md`（D-CY1=A・D-CY3=A・D-CY4=C）。

## 1. MVP の範囲 — Creator Story だけ

**Story ＝ 制作の記録。** 1 本のゲーム（または作りかけ）に紐づく、
時系列の短い記事の束。

MVP に**入れる**もの:

| 機能 | 中身 |
| --- | --- |
| アカウント | ハンドル＋パスワード（GAMEYARD と同じ自前方式を流用）。メールは任意 |
| Story を書く | タイトル・本文（プレーンテキスト＋改行。まず装飾なし）・任意の画像。投稿フォームに任意の 3 枠テンプレ「やったこと / つまずいたこと / 次の一歩」を**見出し挿入の補助**として置く（保存形式は同じ自由文。構造化しない — 空欄が義務に見えると敷居が上がるため） |
| つまずきタグ | 「ツール名」＋「つまずき・トピック」の 2 軸タグ。検索流入の面を作る（ACQUISITION A1）。Story には任意の「つまずき」欄（短文）＋**未解決/解決**の状態を持てる（既定は未解決・切替は本人のみ。解決数の公開カウンタは作らない） |
| 使ったツール欄 | AI を含む使用ツールを普通に書ける定型欄（隠さない文化の実装） |
| 読む | 一覧（新着順）・個人ページ（その人の Story が時系列で並ぶ＝Timeline の原型） |
| 公開範囲 | 公開／下書きの 2 択だけ |
| GAMEYARD への手動リンク | Story に GAMEYARD の作品 URL を貼れる（自動連携は後） |

MVP に**入れない**もの（理由つき）:

| 入れない | 理由 |
| --- | --- |
| Recipe / Help Wanted / Guild / Mentor | 相手が要る。人が集まるまで空の部屋になる |
| 感想・コメント | 初期は書き手が主役。読者機能は書き手が付いてから |
| 決済・販売 | D-CY4=C。外部リンクのみ |
| ランキング・PV 表示 | 文化（数字を競争にしない） |
| GAMEYARD との自動転載・共通アカウント | 最初から共通基盤にすると両方の変更が互いを壊す。手動リンクで始めて需要を見る |
| 多言語 | GAMEYARD と同じく、しない |

## 2. 文化（GAMEYARD から引き継ぐ決まり）

1. 完成度で人を落とさない（未完成・作りかけが主役）
2. 数字を競争にしない（順位表・称号・公開カウンタを作らない）
3. 記録は本人のもの（消す自由。退会したら Story も消える）
4. AI を含む使用ツールを隠させない（書くのが普通、の設計）
5. 個人単位の行動計測をしない（持つのは合計値だけ）

## 3. 構成（GAMEYARD と同じ省力型）

- Next.js App Router。`SITE_MODE=static|server` の 2 モード（GAMEYARD と同型）
- 公開済み Story は静的に書き出して配信、書く側だけ軽い API
- 実行時依存 3 つ（next/react/react-dom）。DB は持たずファイル保存から始める
  （GAMEYARD の store 方式。規模が来たら考える — 来ることが先）
- 配色はアンバー系（工房・電球色。BRAND.md）。レイアウト文法は GAMEYARD と共通

## 4. 撤退条件（先に書く）

公開から 30 日で「書き手 10 人・Story 30 本」に届かなければ、
機能追加を止めて原因を人に報告し、D-CY1 の再判断（B: 保留に戻す）を仰ぐ。
数字はサイト全体の合計のみで計測する。

## 5. 決まっていないこと

- ドメイン（空き確認済み: creatoryard.io / .jp / .net）— 社長
- リポジトリ名の綴り（現 `creater-yard`。ブランドは CreatorYard）— 社長
- 公開時期 — MVP が動いてから判断
