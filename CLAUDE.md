# CLAUDE.md — このリポジトリで作業する Claude へ

**最初に `docs/HANDOVER.md` を読むこと。** このプロジェクトの背景・
これまでの経緯・決定事項・働き方がすべてそこにある。読まずに実装を
始めないこと（過去の決定を知らずに壊すため）。

## 一行で

CreatorYard ＝ ゲームを**作る人**が育つ場所。姉妹サービス
GAMEYARD（https://play-game-yard.com ＝ 作品を届ける場所・本番稼働中）の
「人」側。MVP は **Creator Story（制作記録）のみ**。

## 守ること（GAMEYARD から引き継ぐ決まり）

- **依存パッケージを増やさない**（実行時は next / react / react-dom の 3 つだけ）
- **検査・認証・上限を緩めない**
- **決済を持たない**（収益化は本人の外部リンクのみ。D-CY4=C。
  収益戦略は 2026-08-06 に更新 — `docs/REVENUE.md`。販売系の決済方式は
  社長確認中で、**確認が取れるまで決済の実装に着手しない**）
- **数字を競争にしない**（ランキング・称号・公開カウンタを作らない）
- **個人単位の行動計測をしない**（持つのは合計値だけ）
- **方針を変える判断は人（社長）がする。** AI は判断材料を書き、決定を待つ
- 推測で埋めない。確かめられない数字・実在の個人情報を並べない
- コミットは小さく分け、大きな変更は理由をコードのそばに残す

## 連携の場

- **CreatorYard の AI Review Board = このリポジトリの Issue #1。**
  ChatGPT のレビュー・社長の決定はそこに来る。作業報告もそこへ書く
- GAMEYARD 側の経緯・詳細文書は `tukemen-rgb/site` リポジトリ
  （`docs/creatoryard/` に構想・分析・決定シートの原本）。必要なら
  add_repo で読みに行ける

## 検証

`npm run lint`（ESLint＋型検査）と `npm run build`（静的書き出し）を
必ず通す。画面を触ったらブラウザで実物を見る。
